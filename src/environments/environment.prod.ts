export const environment = {
  production: true,
  apiBaseUrl: 'https://your-production-api.com/api',
  endpoints: {
    credits: '/credits',
    validate: '/validate',
    process: '/process'
  }
};
